{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/node" }  // Backend API routes
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.js" }  // Catches all frontend routes 
  ]
}
